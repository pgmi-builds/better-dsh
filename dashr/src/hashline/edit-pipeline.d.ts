/**
 * @deprecated — private to Mutation seam. Use `from "./mutation.js"` instead.
 * Thin shim re-exporting the pipeline now owned by Mutation.
 * @module dsh-better-edit/edit-pipeline
 */
export * from "./mutation.js";
