# v0.2.1d 实测报告（v0-2-1d-tool-runtime-scheduler-mount 第一人称实测）

- 部署：lib 版本 `0.2.1-d`（工作区 `dashr/package.json` `0.2.1-d`；部署位 lib 逐字节一致，package.json 版本字段漂移见 O-1），profile web；部署同步 18:19:03、daemon 重启 18:38:12，本会话运行于更新后的运行时
- 被测内容：**本 change**（`openspec/changes/v0-2-1d-tool-runtime-scheduler-mount/`）——根因更正（dual-copy）下的「registry 获取保持原样」裁决（D2）、guard 自诊断增强（2.2）、symbol 同一性回归测试（2.4）、活体 4 探针矩阵回归基线（4.1/4.2）
- 实测人：被测 Dash Agent 运行时本体（第一人称）；探针经 `eval` cell 内 `tool.<name>(...)` 直接调用 REPL 子分派桥——与 v0.2.1b 崩溃矩阵、v0.2.1c loud 错误矩阵完全同路径
- 对应任务清单：tasks.md 4.1 / 4.2 / 4.3（以及 2.2 / 2.4 交付物落位复核）——本报告即该核验的记录

## 部署核验（实测前置）

- 版本：工作区 `dashr/package.json` = `0.2.1-d` ✅；部署位 package.json 版本字段仍为 `0.2.1-c`（**元数据漂移，非代码漂移**，见 O-1）
- md5 核验（工作区 `dashr/lib` ↔ 部署位 `~/.dsh/profiles/web/node_modules/@pgmi-builds/better-dsh/lib`）：

| 文件 | 工作区 | 部署位 | 一致 |
|---|---|---|---|
| index.js | `56db6ef89f2750dc8fa4b11fefad7654` | 同左 | ✅ |
| client/index.js | `8b1548613b5d68ef4606f4729b0af312` | 同左 | ✅ |

- `diff -rq lib/` 两侧 **LIB DIRS IDENTICAL** ✅
- 时间线：部署位 lib/index.js mtime `18:19:03` → daemon `Stopping 18:38:11` → `Started 18:38:12`（journal）——**重启晚于同步，运行代码即新 lib** ✅
- 产物内容抽查：v0.2.1d guard 新错误文本在部署 lib/index.js 中逐字在位——含 `Most likely a dual-copy dsh-tools` 诊断、`This plugin resolves @deepseek-ai/dsh-tools from: ${resolvedFrom}` 自诊断路径、`only schemastery and cosmokit belong there` 部署纪律提示 ✅（源码位 `src/index.ts:691-709`，`createRequire(import.meta.url).resolve(...)` 取插件位解析路径）

## §1 4 探针矩阵（4.1 回归核验，第一人称）

对照 v0.2.1b 诊断矩阵（`docs/REPL-工具调用-截断诊断.md` §一）与 v0.2.1c 实测矩阵，同路径 cell 在**本会话（v0.2.1d 运行态）**重跑：

| # | cell 内容 | v0.2.1b | v0.2.1c | v0.2.1d 实测结果 | 判定 |
|---|---|---|---|---|---|
| 1 | `await tool.subagent({… run_in_background: false})` | 截断（崩溃 17:39:17） | loud 错误 | **真实结果**：前台 runId `7c7b6b1e…`，子代理真实执行并输出 `PROBE-OK`（5.4s） | ✅ |
| 2 | `await tool.subagent({… run_in_background: true})` | 截断（崩溃 17:41:15） | loud 错误 | **真实结果**：受理返回 `{"kind":"continuable","subagentId":"90d8e8f9…"}`，子代理异步完成，关闭消息 `4247`（= 137×31，答案正确）——端到端闭环 | ✅ |
| 3 | `await tool.bash({… run_in_background: true})` | 截断（崩溃 17:57:05） | loud 错误 | **真实结果**：job `bash-1`，`job_output` 收割 stdout `PROBE3-BG-OK\nPROBE3-BG-DONE\n`，status completed（2.0s） | ✅ |
| 4 | `await tool.read({path: "dashr/package.json"})` | 截断（崩溃 18:02:27） | loud 错误 | **真实结果**：11591 字符真实内容（hashline 锚点格式），`"version": "0.2.1-d"` 可见 | ✅ |

- **4/4 全部返回真实执行结果**——既非 v0.2.1b 的截断/崩溃，也非 v0.2.1c 的 loud `ToolCallError`。三版本同路径对照：崩溃 → 可见错误 → **真实可用**，递进完整。
- 探针 2 的响应形态为 continuable subagent（durable `subagentId`）而非 jobId，`job_output` 不适用；子代理结果经 runtime 落地通知回收（本会话已收到，payload 正确）——与 job 型后台（探针 3 的 `bash-1`）是两种正常形态，记入 O-2。

## §2 journald 守护进程日志（4.2 铁证）

- 自重启时刻 `18:38:12` 起（探针全部完成后于 18:43 复扫）：`journalctl --user -u dsh.service --since "18:38:12"` grep `fatal|TypeError|FAILURE|crashed|exit(1)` → **0 条匹配**；journal 仅 19 行正常启动 INFO（MCP 注册、`Successfully registered all tools`、dsh web URL）。
- 对照：v0.2.1b 的 17:39:17 / 17:41:15 / 17:57:05 / 18:02:27 四条 fatal load failure 已不再出现；v0.2.1c 时点的 loud 错误路径本轮探针一次都未触发（4/4 走正常调度器）。
- 4 探针执行期间及之后 daemon 持续存活（pid 1951131），后续 cell 正常执行——探针不再是「进程杀手」，而是普通工具调用。

## §3 根因更正与交付边界复核（design Context/D2 活体确认）

- **4/4 真实结果即根因更正的活体终验**：现行生产拓扑（① 层 symlink 清理后）下 `runtimeCtx.tools` plain read 返回带 `[TOOL_RUNTIME_SCHEDULER]` 的原生实例——design「shadow 视图从未在活体上出现、真实根因是部署拓扑 dual-copy」的裁决由本轮矩阵再次确认。候选 A/B/C/D 无需实施，registry 获取保持 `const registry = runtimeCtx.tools` 原样（源码走查未见改动，符合 D2）。
- **v0.2.1d 交付物落位**：
  - guard 自诊断（2.2）：源码 `src/index.ts:691-709` + 部署产物逐字在位；本轮 4/4 探针正常路径未触发该分支（预期行为——guard 只在 dual-copy 复发时发声）。
  - symbol 同一性回归测试（2.4）：`test/bridge.spec.ts:586` 用例 `v0.2.1d — the mounted ToolRuntime is keyed by the SAME symbol this plugin imports (dual-copy invariant)` 在位，断言经本文件 import 的 symbol 读出 scheduler 且 `prepare/dispatch/finalize/finish` 四方法为函数。
- 交付边界：本 change 的目标「REPL `tool.*` 子分派真实执行并返回结果」**已在活体上达成**；防御纵深保留（v0.2.1c 判空 guard + v0.2.1d 自诊断文本）作为未来 dual-copy 事件的第一现场。

## §4 测试套件

- `npx vitest run`（本轮实测重跑）：**31 files、400 passed = 400 绿、exit 0**（127.95s）——399 旧用例 + 1 新 symbol 同一性用例，与 tasks.md 2.2 记录一致；含 v0.2.1c 的 3 个韧性回归用例与 v0.2.1d 的 dual-copy invariant 用例，本轮活体行为（真实调度、guard 未触发、零崩溃）与套件断言一一对应。

## 观察（非缺陷）

| # | 级 | 观察 |
|---|---|---|
| O-1 | 记录 | 部署位 package.json 版本字段仍为 `0.2.1-c`（工作区 `0.2.1-d`）——部署同步只携带 `lib/`，版本元数据漂移与既有 profile 锁版本漂移同类；lib 已逐字节一致，无碍运行，建议下次部署同步时连带 package.json |
| O-2 | 记录 | 探针 2 后台 subagent 的响应形态是 continuable（`subagentId`）而非 job（`jobId`），收割走 runtime 落地通知而非 `job_output`；探针 3 后台 bash 则是 job 形态——两种后台形态并存为设计使然，报告记录以免后续误判 |
| O-3 | 记录 | agent 的 bash 执行环境内 `systemctl --user` 总线不可达（`No data available`），daemon 状态改经 `journalctl --user -u dsh.service` 取证成功——环境观察，非被测对象问题 |

## 结果

**总体判定：v0.2.1d 的目标在活体 daemon 上全部成立——REPL `tool.*` 子分派真实可用（4 探针矩阵 4/4 返回真实执行结果，前台/后台/bash/读写四形态全覆盖）；自重启（18:38:12）起 journal 0 条崩溃；guard 自诊断与 symbol 同一性回归双交付物在源码、产物、测试三层落位；测试套件 400 全绿。**

- tasks.md 4.1（4 探针 4/4 真实结果）✅、4.2（journal 0 崩溃）✅、4.3（本报告）✅；2.2 / 2.4 交付物复核 ✅。
- 三版本递进就此闭环：v0.2.1b（崩溃复现）→ v0.2.1c（不崩溃 + loud 错误）→ **v0.2.1d（真实可用 + dual-copy 根因更正 + 自诊断防御）**。根因（部署拓扑 dual-copy symbol 分裂）已由 symlink 清理消除，不变量已由回归测试与 guard 自诊断双锚锁定。
