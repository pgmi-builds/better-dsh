# v0.2.1c 实测报告（v0-2-1c-repl-tool-crash-fix 第一人称实测）

- 部署：版本 `0.2.1-c`（package.json，工作区与部署位一致），profile web；用户已执行部署同步 + daemon 重启（19:23:19），本会话运行于更新后的运行时
- 被测内容：**本 change**（`openspec/changes/v0-2-1c-repl-tool-crash-fix/`）的六.1（drive 加 catch）+ 六.2（scheduler 防御）在活体 daemon 上的回归核验基线（六.5）；六.3 根因归属复核
- 实测人：被测 Dash Agent 运行时本体（第一人称）；探针经 `eval` cell 内 `tool.<name>(...)` 直接调用 REPL 子分派桥——与 v0.2.1b 崩溃复现矩阵完全同路径
- 对应任务清单：tasks.md 6.1 / 6.2 / 6.3（以及 3.2 活体复核）——本报告即该核验的记录

## 部署核验（实测前置）

- 版本：`dashr/package.json` 与部署位 `~/.dsh/profiles/web/node_modules/@pgmi-builds/better-dsh/package.json` 均为 `0.2.1-c` ✅
- md5 核验（工作区 `dashr/lib` ↔ 部署位 lib）：

| 文件 | 工作区 | 部署位 | 一致 |
|---|---|---|---|
| index.js | `861fb9d3fb108015fb1d665d00d789b2` | 同左 | ✅ |
| client/index.js | `8b1548613b5d68ef4606f4729b0af312` | 同左 | ✅ |

- `diff -rq lib/` 两侧 **LIB DIRS IDENTICAL**（含删除陈旧 chunk 后的完整目录对比）✅
- lib 产物内容抽查：六.2 loud 错误文本（`the tools service resolved by this composition lacks the TOOL_RUNTIME_SCHEDULER symbol …`）在 index.js 中出现；六.1 lane backstop（`settling queued dispatches as errors`）在源码 `src/index.ts:642-652` 在位 ✅
- daemon 重启：journal 显示 19:23:19 旧进程（pid 443984）SIGTERM 优雅停止 → 同一秒新进程（pid 532389→532407→532567）启动；自重启至今 **0 条 fatal / TypeError / FAILURE / restart** ✅

## §1 4 探针矩阵（六.5 回归核验，第一人称）

对照 v0.2.1b 诊断矩阵（`docs/REPL-工具调用-截断诊断.md` §一），同 cell 内容在**本会话（v0.2.1c 运行态）**重跑：

| # | cell 内容 | v0.2.1b 结果 | v0.2.1c 实测结果 | 判定 |
|---|---|---|---|---|
| 1 | `await tool.subagent({… run_in_background: false })` | 截断（daemon 崩溃 17:39:17） | cell 收到 `ToolCallError`，消息点名 `TOOL_RUNTIME_SCHEDULER` symbol + scope/service view 上下文 | ✅ 返回结果非截断 |
| 2 | `await tool.subagent({… run_in_background: true })` | 截断（daemon 崩溃 17:41:15） | 同上 | ✅ 返回结果非截断 |
| 3 | `await tool.bash({… run_in_background: true })` | 截断（daemon 崩溃 17:57:05） | 同上 | ✅ 返回结果非截断 |
| 4 | `await tool.read({path: "dashr/package.json"})` | 截断（daemon 崩溃 18:02:27） | 同上 | ✅ 返回结果非截断 |

- **4/4 全部返回可见结果**（loud `ToolCallError`，含完整上下文消息），**零截断、零 daemon 崩溃**。每次探针后 daemon 继续存活、后续 cell 正常执行——与 v0.2.1b 的「探针即杀进程」形成对照。
- 探针实际收到的错误消息（4/4 逐字一致）：
  > `eval: the tools service resolved by this composition lacks the TOOL_RUNTIME_SCHEDULER symbol (a scope/service view instead of the native ToolRuntime instance) — REPL sub-dispatch cannot run; check the harness mount wiring (packages/core/tools) resolving the plugin's tools service`
- **对照结论**：v0.2.1b 同路径的「裸 `undefined.prepare` TypeError → driver 无 catch → unhandled rejection → installFailLoud fatal → daemon exit(1) → repair 合成 interrupted-tool-result」链条已被六.1+六.2 打断。错误现在**在 cell 内可见**，binding promise 正常 settle，进程零外泄。

## §2 journald 守护进程日志（六.5 铁证）

- 自重启时刻 19:23:19 起：`journalctl --user -u dsh.service --since "19:23:19"` grep `fatal|TypeError|FAILURE|restart|crashed|exit` → **0 条匹配**；journal 尾部仅启动 INFO（MCP 注册、dsh web URL）。
- 对照：v0.2.1b 的 17:39:17 / 17:41:15 / 17:57:05 / 18:02:27 四条 fatal load failure（`Cannot read properties of undefined (reading 'prepare')`）**不再出现**。
- 会话日志侧：本会话（`session-6c013361…`）**0 条 `interrupted-tool-result`** repair 事件——无截断指纹。且因六.2 guard 在 `binding()` 内、`start()` 之前抛错（`src/index.ts:695-697` 先于 `tool/code-dispatch-start` append），本次探针连半程 dispatch 日志都没有留下——错误在最早点被拒绝，不产生部分状态。✅

## §3 根因归属与交付边界（六.3 活体复核）

- 活体探针结论：**生产组合仍把 `runtimeCtx.tools` 解析为缺 `TOOL_RUNTIME_SCHEDULER` 实例 symbol 的 scope/service 视图**——4/4 探针的 loud 错误本身就是活体证据（错误消息由 guard 在 symbol 判空时抛出，证明运行态确实取不到 symbol）。
- 与诊断文档 §四/§七的推演一致：测试组合（根作用域原生实例，symbol 在位）全绿，生产组合（preset realm / plugin tree 发布路径带 shadow 标记 → `getTraceable` 返回原型而非实例）缺 symbol——**差异在 harness 侧（dsh-alpha），作为外部依赖记录**。
- **本 change 的 dashr 交付边界 = 「不崩溃 + loud 错误」——已达成**（§1+§2 实证）。「调度器真正可用、REPL 子分派真实执行」取决于 harness 挂载修正（dsh-alpha），不在本 change 交付物内。

## §4 测试套件

- `npx vitest run`（本轮实测重跑）：**31 files、396 passed | 3 skipped = 399 绿、exit 0**（109.41s）——与 tasks.md 记录的 399 全绿一致，含 3 个 v0.2.1c 韧性回归用例（缺 symbol 视图 → loud 错误；`scheduler.prepare` 抛错 → binding settle 可见错误；classify 抛错 → lane backstop settle 且进程存活）。本轮实测的活体行为与这三个用例的断言一一对应。

## 观察（非缺陷）

| # | 级 | 观察 |
|---|---|---|
| O-1 | 记录 | REPL 内 `tool.*` 子分派目前**不可用**（调度器 symbol 缺失，loud 错误可见）——这是 harness 侧外部依赖，非本 change 回归；dashr 侧防御保证了「不可用」以可见错误呈现而非进程崩溃 |
| O-2 | 记录 | 本次守护进程重启（19:23:19）由用户执行，重启后本会话续跑于新 lib（部署位 md5 与工作区一致）；journal 无旧崩溃残留以外的任何异常 |

## 结果

**总体判定：v0.2.1c 的目标在活体 daemon 上全部成立——六.1（drive 加 catch）阻断 unhandled rejection 外泄、六.2（scheduler 防御）把裸 TypeError 转为 cell 可见 loud 错误；4 探针矩阵 4/4 返回结果而非截断，journald 自重启后 0 条崩溃，会话日志 0 条 interrupted-tool-result 修复事件。**

- tasks.md 6.1（4 探针矩阵 4/4 非截断）✅、6.2（journal 无新崩溃）✅、6.3（本报告）✅、3.2（活体复核：根因确认在 harness，dashr 可及范围防御已实施，harness 侧记录为外部依赖修复）✅。
- 交付边界已按设计执行：dashr 侧「不崩溃 + loud 错误」；「调度器真正可用」依赖 dsh-alpha 挂载修正，需在 harness 侧 change 中跟进。
